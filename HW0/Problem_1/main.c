#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "bigint.h"

#define MAX_DEC 256

int main()
{
    char a[MAX_DEC], b[MAX_DEC];

    scanf("%s", a);

    //bigint* I = newnumc(a[1]);

    return 0;
}